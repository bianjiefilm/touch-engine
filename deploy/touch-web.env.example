# touch-engine web env sample (HUI-1746 T0)
# 目标路径(生产惯例):/etc/touch-engine/touch-web.env (0640, 不进 git)
# Next.js BFF 只指向 loopback 的 Go 服务;浏览器永远不直接访问 Go 服务或平台服务。

# Go 服务地址(loopback)
TOUCH_SERVER_URL=http://127.0.0.1:18240
# 与 touch-server.env 中 TOUCH_INTERNAL_TOKEN 相同的共享内部令牌
TOUCH_INTERNAL_TOKEN=
